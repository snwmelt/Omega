namespace Omega.Model.Enum
{
    /// <summary>
    /// Enum for indicating Transaction instance state.
    /// </summary>
    internal enum TransactionState
    {
        Outstanding = 0,
        Resolved    = 1
    }
}
