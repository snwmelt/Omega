<Window x:Class="Omega.View.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:c="clr-namespace:Omega.View.Control"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Omega.View"
        mc:Ignorable="d"
        Title="MainWindowView" Height="768" Width="1024">    
    <Grid SnapsToDevicePixels="True">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid x:Name="AccountsListGrid" Grid.Column="0" Width="225">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <c:ModernTextBox x:Name="AccountsSearchBox" BorderThickness="0,0,0,2" Grid.Row="0" Height="28" Margin="2" PlaceholderText="Add/Search Account" TextAlignment="Center" VerticalContentAlignment="Center"/>

            <ListView x:Name="AccountsListListView" BorderThickness="0,2,2,0" Grid.Row="1" Margin="2"/>
        </Grid>

        <Grid x:Name="AccountDataGrid" Grid.Column="1">
            
        </Grid>
    </Grid>
</Window>
